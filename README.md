# Node Micro Boilerplate

Minimal starter files to create a Node.js (TypeScript powered) app.

## Docker

### Build

```bash
docker build -t boilerplate .
```

### Run

```bash
docker run --rm -it --name boilerplate boilerplate
```

### Access

```bash
docker exec -it boilerplate /bin/sh
```
